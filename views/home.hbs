{{>Navbar }}

  <div class="w-11/12 mx-auto mt-5">
    <div class="grid 2xl:grid-cols-5 xl:grid-cols-5 lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2 grid-cols-1 justify-center">
      {{#each notes}}
        {{>Card item=this}}
      {{/each}}
    </div>
  </div>

